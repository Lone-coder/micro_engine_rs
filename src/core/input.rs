use sdl2::keyboard::Keycode;
use sdl2::EventPump;

pub struct Input {

}

impl Input {
    pub fn is_key_pressed(key_code : &mut Keycode) -> bool {
        false
    }

    pub fn process_events(event_pump : &mut EventPump) {

    }

}
